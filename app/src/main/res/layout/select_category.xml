<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="23dp"
            android:id="@+id/select_category_return"
            android:layout_alignTop="@+id/select_category_type"
            android:src="@mipmap/return_icon"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="选择类别"
        android:textSize="14sp"
        android:id="@+id/select_category_type"
        android:layout_marginTop="15dp"
        android:layout_centerHorizontal="true"/>
    <TextView
        android:paddingStart="23dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/select_category_mainStyle"
        android:text="主营类别：（最多选择3种）"
        android:layout_below="@id/select_category_return"
        android:layout_marginTop="10dp"
        android:textSize="14sp"/>
    <ListView
        android:id="@+id/select_category_listView"
        android:layout_width="wrap_content"
        android:layout_above="@+id/select_category_btn"
        android:layout_below="@id/select_category_mainStyle"
        android:dividerHeight="1dp"
        android:divider="@color/gray"
        android:layout_marginBottom="30dp"
        android:layout_height="wrap_content"></ListView>

    <Button
        android:layout_width="match_parent"
        android:background="@drawable/round20dp"
        android:text="保存"
        android:textSize="18sp"
        android:layout_alignStart="@id/select_category_return"
        android:layout_marginEnd="23dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="20dp"
        android:id="@+id/select_category_btn"
        android:textColor="@color/whiteBgColor"
        android:layout_height="wrap_content" />

</RelativeLayout>